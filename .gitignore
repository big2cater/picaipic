# IDE / editor
.vscode/
.continue/

# Rust build output
.cargo/
target/

# Frontend dependencies
src-vite/node_modules/
**/package-lock.json

# Build artifacts (signed plugin zips, staging, etc.)
dist/

# Runtime plugin data (created at dev time by the running app)
src-tauri/picaipic-local/

# Skills cache
skills/

# Local brainstorm / visual companion sessions
.superpowers/

# OS
.DS_Store

# Secrets — NEVER commit
.env
picaipic-release-key.txt
*-sign-key.txt
*private-key*.txt
picaipic-updater-key.key
picaipic-updater-key.key.pub
*.updater-key.key
*.updater-key.key.pub

# Local scripts / personal tooling
dev.sh
build.sh
set_version.py
publish_release.sh
release-downloads.sh

# Phase 0 model probes (large ONNX; never commit)
scripts/.probe-models/
scripts/probe_siglip2_ort/target/
scripts/probe_siglip2_ort/Cargo.lock

# Temporary debugging artifacts
tmp_*
tmp_*/
nul

# Python bytecode cache
__pycache__/
*.py[cod]

# ZCode client local state
.zcode/
tools/

# Local dev leftovers (never commit)
.pnpm-store/
target-codex-*/
favicon (8).ico
picaipic-profile-screen.png
scan-*.log
