---
name: router
description: PicAiPic session bootstrap, current state, and context routing hub.
edges:
  - target: context/architecture.md
    condition: when understanding application flow or changing a major subsystem
  - target: context/stack.md
    condition: when working with dependencies, versions, or build tooling
  - target: context/conventions.md
    condition: when writing or reviewing Rust, Vue, IPC, database, or UI code
  - target: context/decisions.md
    condition: when a non-obvious design choice or historical rationale matters
  - target: context/setup.md
    condition: when running, validating, packaging, or troubleshooting the development environment
  - target: context/plugin-runtime.md
    condition: when touching AI plugins, manifests, runtimes, permissions, tasks, packaging, or sandboxing
  - target: patterns/INDEX.md
    condition: before any implementation or diagnosis task
last_updated: 2026-09-02
---



# Session Bootstrap

Read root `AGENTS.md`, then this file, then the routed context and matching pattern before changing code.

## Current Project State

**Working:**
- **AI plugin UI hide toggle (2026-09-02):** Settings → Plugins tab now has a "Third-party AI plugins" switch (`config.aiPlugin.hidden`, default false). When hidden: `pluginStore.getMenuItems` returns empty (the single source for every plugin context-menu item), `loadPlugins` short-circuits so the host is never probed on startup, and the plugin management panels (store location, list, logs) are hidden via a `<template v-if>` — the ComfyUI settings block is a separate section and stays visible. Re-enabling reloads the plugin list. Frontend build passes.
- **Editor save / file-move Windows flush fix (2026-09-02):** report "保存图片失败。不支持的格式或文件已损坏。" on every image edit save traced to `edit_image`'s flush step — `File::open(&temp).sync_all()` used a read-only handle, and Windows `FlushFileBuffers` rejects read-only handles with access-denied (os error 5); Linux tolerates read-only fsync, which is why it passed CI. Same latent bug in `t_utils.rs` move-file staging. Both now flush through a read-write `OpenOptions` handle (t_image.rs `edit_image`, t_utils.rs staged-move sync). New hermetic `edit_save_pipeline_tests` cover save-as-new (new dest file, source untouched) and overwrite (source replaced); PNG/WebP-source-to-JPEG saves also verified tolerant (metadata copy handles EXIF-less sources). Full Rust suite 192 passed / 3 ignored. Rule: never fsync through `File::open` on Windows.
- **ComfyUI integration review sweep (2026-09-02):** `comfy_download_output`'s intermediate file was `.{name}.picaipic-comfy-*.tmp` — a leading dot escaped `is_allowed_temp_basename`, so interrupted downloads leaked permanently; it is now `picaipic-comfy-<uuid>.tmp` (sweepable) and self-removes when the final rename fails. `comfy_run_workflow` now checks the HTTP status before parsing (4xx reported as status + text; 200+`node_errors` unchanged). Documented limitation: only node-output `images` arrays are collected — animated/video outputs are skipped.
- **ComfyUI integration fixes (2026-09-02):** UI-format misdetection fixed and hardened in two layers — `isUiWorkflow` requires `widgets_values`, and `normalizeApiWorkflow` (`src-vite/src/common/comfyConvert.js`) rebuilds API-format exports that keep a stray `nodes` array (no `widgets_values`) into the clean keyed form, so the offending file neither gets corrupted by a second conversion nor rejected as `not_api_format`; stray `nodes`/`links` keys never reach `/prompt`. Workflow save (`Settings.vue saveComfyWorkflow`) is now an upsert by name (id kept) so re-importing a corrected file replaces the stale entry instead of creating a duplicate that the picker defaults to; the import dialog shows an overwrite hint. Plus results UX (same day): outputs import under readable names `comfy_<workflow>_<n>.<ext>` (`import_file` gained `target_name`; `t_utils::sanitize_import_name` is the sanitization authority with unit tests), and after a run Content auto-scrolls + selects the first result via `revealImportedFile` (shared with dedup: `resolveFileIndexInCurrentQuery`, waits for reload settle, album-scoped guard) — also wired into batch saveAs, photo frame, print layout, and plugin result imports (grid dialog + viewer message). Verified with shape fixtures + full Rust suite 187 passed / 3 ignored + frontend build — live status `docs/comfyui-integration-status.md`, runbook `patterns/change-comfy-integration.md`.
- **Upstream Lap v0.3.1 features ported (2026-08-29):** (1) **Album folder search** — ported the upstream design: `AFolder::get_all_for_search` + the `get_all_album_folders` command return every folder of every album, and `AlbumList` builds a filtered tree from that flat list (`getFolderSearchPaths` collects matches plus their ancestor chain, `buildFilteredFolderTree` relinks nodes by path prefix and applies `folderSort`). `AlbumFolder` takes `filterVisiblePaths`/`filterMatchedPaths` props and forwards them recursively; while filtering it renders `visibleChildren` and **ignores `is_expanded`**, so a collapsed ancestor still reveals its matched branch — matched folders highlight, ancestors dim as context, and no `v-html` is involved. This avoids touching the lazy-loading tree at all. (2) **Dedup review** — `set_keep_in` now treats `file_id <= 0` as *unkeep*: the keeper is cleared and the group returns to all-selectable, which was previously impossible. The panel gained an Unkeep button on the keeper and a Reanalyze button (forces `triggerBackendDedup(true)`, bypassing the per-mode scan-key memo). Selected-item deletion already existed. (3) **Phase One IIQ RAW** — added `iiq` to `RAW_IMGS`, and fixed the underlying reason such formats had to be excluded: `AFile::new_profiled` used `?` on `get_raw_dimensions`, so any RAW LibRaw could not decode aborted the entire row. It now keeps the file indexed with unresolved dimensions and logs, which also allows `x3f` to be re-enabled. Also added reusable `SidebarSearch.vue` with panel-scoped filtering for **Smart Albums** and **People** (Tags already had its own). Verified against upstream: Lap v0.3.1/v0.3.0 are otherwise already covered here — Smart Albums, Collections, Live Photo, 4-up compare, media badges, AI prompt import, AI search file-type filters, viewer backgrounds, hover playback, multi-file external open, commit hash in About, folder favorite/counts, search-excluded folders, physical-key shortcuts, inode tracking, and cross-album thumbnail relocation all pre-exist. (4) **Inline subfolder creation** — the folder context menu now opens an in-tree name input instead of a modal: a placeholder row (`id: -1`) is inserted under the parent with an auto-unique name, Enter or click-away confirms via `createFolder` + refetch (so the real row replaces the placeholder), Esc removes it. State is shared across the recursive component through `provide`/`inject` (`NEW_FOLDER_CONTEXT`) so only one input can ever be open. (5) **Related Photos grouping strictness** — the dHash Hamming distance that groups the Similar Photos panel was hard-coded (`DHASH_HAMMING_THRESHOLD = 8`). It is now `SIMILAR_GROUPING_DISTANCES` (6/8/12 = strict/normal/loose), chosen by `config.dedup.similarGrouping` and plumbed through `dedup_start_scan` → `start_scan` → `scan_and_phash_files` → `rebuild_similar_groups` → `build_similar_group_plans`. It deliberately stays independent of the AI-search cosine threshold behind "Find Similar Photos", which is the point of the upstream change. Exact duplicates are now excluded from related-photo analysis (`NOT IN (SELECT file_id FROM duplicate_group_items)`) so byte-identical pairs are not reported twice. Every item from upstream Lap v0.3.1 / v0.3.0 is now covered.
- **Destructive-op / trust-boundary bug sweep (2026-08-29):** `t_image.rs` collage, batch, and photo-frame exports no longer delete an existing destination before publishing their temp — `replace_output_file`/`finalize_tracked_output` stage the old file to a same-directory backup and restore it when both rename and copy fail, closing a data-loss window (IMG-6). `move_file` treats a zero-row DB update as a failure and rolls the disk move back; `rename_file`/`rename_folder` keep only the final component of a new name; a superseded destination now goes to the **system trash** instead of `remove_file`, so Replace is recoverable like `delete_file`; staged deletes are journaled through `TrackedOutputTemp::create_staged_delete` so a crash restores the media instead of leaving an invisible `.lap-delete-*` orphan; storage migration and backup remove their temp artifacts on early failure; `edit_image` returns `Result<(), String>` so failures are diagnosable (IMG-7). Plugin side: stop/uninstall only reaps a port the host actually spawned (never `taskkill` on a manifest port), bearer auth resolves through fail-closed `resolve_plugin_auth_token` (managed runtimes must present their token; external `baseUrl`-only services stay anonymous by design), and input staging copies instead of hardlinking unless the manifest declares `writeSourceFiles`. Full Rust suite 183 passed / 3 ignored; Rust fmt/check and frontend build pass — `patterns/audit-image-edit-save.md`, `patterns/change-destructive-file-ops.md`, `patterns/change-database-schema.md`, `patterns/change-ai-plugin.md`. Known note: Replace/trash unit tests now move small `%TEMP%` fixtures to the real recycle bin.
- **Dynamic-theme documentation closure (2026-07-30):** English/Chinese README, development status, v1.1.0 notes, black-hole design, cyberpunk spec/plan, and release guidance now agree on the seven-way idle gate, Home-owned native maximize sync, legacy intensity migration, GPU buffer limits, theme-specific CSS photo fallbacks, reduced-motion behavior, install-path independence, and same-version MSI cache. Runtime guide: `docs/guide/fx-theme-runtime-compatibility.md`.
- **Cross-GPU cyberpunk photo fallback (2026-07-30):** `PhotoGlitchLayer` now clamps high-DPI capture/render buffers to WebGL texture and viewport limits, rejects capture/upload failures, and reports context loss. GridView keeps live photos visible and activates a CSS translation/color/RGB-edge glitch when WebGL cannot run, matching the black-hole failure strategy — `patterns/change-cyberpunk-theme.md`.
- **Cross-GPU black-hole photo fallback (2026-07-30):** `PhotoVortexLayer` now clamps high-DPI capture and render buffers to the active GPU's WebGL texture/viewport limits, rejects empty captures and GL upload errors instead of hiding the grid behind a black texture, and reports context loss. GridView switches those failures to the existing per-card CSS spiral, preserving visible photo distortion on constrained laptop GPUs/WebView2 versions — `patterns/change-black-hole-theme.md`.
- **Cross-machine FX theme activation fix (2026-07-30):** legacy settings missing `dynamicThemeIntensity` now migrate to normal intensity 1 without overriding an explicit off value, and Home independently synchronizes the main Tauri window's native maximize state on startup/resize. This removes machine/install-history-dependent black-hole and cyberpunk idle activation; Windows reduced-motion remains an intentional disable gate — `patterns/change-black-hole-theme.md`, `patterns/change-cyberpunk-theme.md`.
- **Install-path-independent runtime resources (2026-07-30):** MSI layout verification confirms bundled models, FFmpeg/FFprobe, branding, and embedded frontend assets resolve from the Tauri resource directory regardless of install drive/name. Release plugin storage now defaults to writable Local AppData instead of the executable directory, while an existing legacy install-adjacent store remains visible until explicitly migrated — `context/plugin-runtime.md`, `patterns/change-ai-plugin.md`, `patterns/release-build.md`.
- **Windows installer freshness diagnosis (2026-07-30):** MSI administrative extraction confirms the bundled exe contains the same current frontend asset hashes as the release build output; the three-byte executable difference is only Tauri's `MSI` versus restored `UNK` bundle marker. Packaging now closes PicAiPic processes from build and installed paths, keeps the generated override's repository-root-relative `src-vite` command, and warns that same-version MSI retests require uninstall/version bump — `patterns/release-build.md`.
- **Plugin package mutation serialization (2026-07-30):** install and uninstall now hold one fail-fast process-wide RAII guard across their filesystem and registry transaction, preventing concurrent IPC installs from overwriting each other's registered paths. The existing Settings in-progress guard remains UX defense; the backend guard is authoritative — `patterns/audit-plugin-install-rollback.md`, `patterns/change-ai-plugin.md`.
- **Scan resume warning cleanup (2026-07-30):** removed the unused `traversed_count` counter left behind when unsafe progress-prefix skipping was replaced by full survivor re-marking. Scan progress/checkpoint/sweep behavior is unchanged; Rust format/check pass — `patterns/audit-scan-mark-sweep.md`.
- **Frontend performance audit closure (2026-07-30):** `Content.vue` now incrementally drops off-screen base64 thumbnail references outside a bounded visible/prefetch window while the existing 96 MiB shared LRU preserves fast back-scroll. Plugin contribution lookup is shared once per GridView; file-specific menu state remains per card. PE-3 repeated O(1) visible-slot getters are intentionally unchanged, and PE-4's idbKeyval claim is stale. Frontend build and diff check pass — `patterns/audit-frontend-perf.md`, `patterns/change-library-perf.md`.
- **Cyberpunk audit closure (2026-07-30):** `setTheme` now owns a global `html.is-cyberpunk` class and `app.css` overrides the daisyUI 5 primary variables, so neon chrome remains consistent across Home, viewer, editor, settings, and auxiliary routes. The glitch paint loop schedules only while active with a valid GL program and texture. Reusing dark variables and keeping the animated city Home-only are confirmed product boundaries. Theme-id check, frontend build, compiled-CSS inspection, and diff check pass — `patterns/audit-cyberpunk-theme.md`, `patterns/change-cyberpunk-theme.md`.
- **Export temp crash recovery (2026-07-30):** batch, collage, and photo-frame writes now persist a synced exact-path journal before creating UUID-namespaced same-directory temps. RAII handles normal/error/cancel/task-abort cleanup; startup validates journal filename, canonical UUID, output kind, and temp suffix before deleting only the registered path. Corrupt/ambiguous entries are retained and logged; arbitrary export roots are never swept. Focused recovery tests: 3 passed; full Rust suite 174 passed / 3 ignored; Rust format/check and frontend build pass — `patterns/audit-image-edit-save.md`, `patterns/change-batch-process.md`.
- **Outside-library move crash recovery (2026-07-30):** `move_file_outside_library` now persists a synced, original-library-bound journal before disk mutation, uses deterministic destination-side staging/backup paths, holds the media writer guard, and deletes through the captured library connection. Startup reconciliation rolls back prepared moves or completes DB deletion for committed disk moves; ambiguous/external changes are retained and logged. Three recovery tests pass; full Rust suite 171 passed / 3 ignored; Rust format/check and frontend build pass — `patterns/audit-file-move-import.md`, `patterns/change-destructive-file-ops.md`.
- **Destructive audit summary re-verification (2026-07-30):** the stale 2026-07-29 severity queue is replaced by a current closure matrix. All actionable correctness findings are resolved or verified; intentional `copy_file` transfer semantics and backend library-rebinding correctness are recorded to prevent regressive fixes. Remaining items are bounded follow-ups: 100k-face RSS measurement, unknown export-root temp hygiene, and the accepted checkpoint snapshot trade-off — `patterns/audit-summary.md`.
- **Frontend destructive audit closure (2026-07-30):** plugin package selection/install now has a dedicated in-progress guard across trust retry and refresh; filesystem-path and byte-fallback drag imports report partial failures; image-edit IPC errors remain observable. Re-verification classifies library-switch and edit consistency findings as closed by backend exclusive rebinding/atomic metadata refresh, and retains `copy_file` + explicit library indexing by design — `patterns/audit-frontend-destructive.md`.
- **Plugin install/uninstall rollback closure (2026-07-30):** replacement stages the old managed code directory to a same-parent backup and restores it when code placement, private storage setup, model inspection, or registry persistence fails; fresh private roots are removed on rollback without touching existing data. Uninstall stages code and optional private data until registry cleanup commits, restores them on failure, and ignores interrupted hidden transaction directories during discovery. Registry writes now use synced temp + backup replacement; package manifests have declared unpacked-size ceilings and transactional deletion uses bounded retry for Windows handles. PLUG-1 through PLUG-5 are closed — `patterns/audit-plugin-install-rollback.md`, `patterns/change-ai-plugin.md`.
- **Library switch/remove isolation (2026-07-30):** backend library rebinding is exclusive with full indexing, dedup, face indexing, and imports. This prevents a current-library pointer change from redirecting any in-flight DB writes to another library. Remove now also clears pooled connections and the embedding matrix, initializes the replacement current DB, and refreshes scopes/mtime sync — `patterns/audit-library-switch-remove.md`.
- **Scan mark-and-sweep recovery hardening (2026-07-30):** current schemas already cascade thumbnail/hash cleanup; the sweep now also removes those derived rows explicitly to repair legacy database states. Crash recovery no longer skips a progress-count prefix: it revisits and re-marks every surviving file before sweeping. A scan that explicitly omits a suspected bad file suppresses the sweep, retaining its index row until a clean full scan — `patterns/audit-scan-mark-sweep.md`.
- **File transfer / clipboard import audit (2026-07-30):** post-move thumbnail relocation, Live Photo pairing, and album recount failures now log file/album context instead of being silently discarded. Clipboard imports return successful files with a failed-count and the UI reports partial success. `copy_file` is intentionally a transfer primitive; shipped library-copy callers index the result and remove it on index failure. The process-crash window between an outside-library disk move and DB deletion remains an explicit recovery-design follow-up — `patterns/audit-file-move-import.md`.
- **Face write / clustering transaction hardening (2026-07-30):** face scan batches now stop and surface `face_index_finished.error` on transactional failure rather than silently advancing. Clustering builds a lock-free plan then atomically creates persons and assigns unassigned faces; cancellation, conflict, or write failure rolls it all back. Shared SQLite connections already have a 5-second busy timeout. Full embedding load remains a measured 100k-face memory follow-up. Face transaction tests: 2 passed; cluster tests: 15 passed; full Rust suite: 161 passed / 3 ignored; frontend build passes — `patterns/audit-face-cluster-tx.md`, `patterns/change-face-index.md`.
- **Storage migration / backup hardening (2026-07-30):** custom DB moves require a completed WAL checkpoint, then copy to a unique target temp, compare SHA-256 and run SQLite `quick_check` before the config switch; prior targets roll back on config-save failure and old DB/WAL/SHM cleanup is staged afterward. Backup and restore stream database bytes, new ZIP entries use library IDs declared in metadata (legacy name entries remain readable), and the obsolete app config file was removed. Checkpoint-based backup remains an accepted concurrent-write snapshot trade-off — `patterns/audit-storage-migration.md`, `patterns/change-database-schema.md`.
- **Plugin package trust-boundary closure (2026-07-30):** declared-but-missing package files are rejected with regression coverage; runtime network permission registry errors deny access; install snapshots the selected ZIP under the managed plugin root and reuses one archive for manifest/hash/signature/trust validation and extraction, closing the source-path swap window. Plugin-host regression passes; full Rust suite: 157 passed / 3 ignored; frontend build passes — `patterns/audit-plugin-trust-boundary.md`, `patterns/change-ai-plugin.md`.
- **Atomic image edit save + index refresh (2026-07-30):** ImageEditor/FileInfo overwrite saves now encode + preserve metadata in a same-directory temp, stage the original, and restore it on finalize or DB-refresh failure. Validated `fileId` refresh updates file metadata transactionally and invalidates thumbnail, embedding, exact-hash, and dHash state. Edit rollback tests: 2 passed; full Rust suite: 154 passed / 3 ignored; frontend build passes — `patterns/audit-image-edit-save.md`.
- **Dedup destructive-delete consistency (2026-07-30):** dedup trash now stages media, revalidates keep/selection under `BEGIN IMMEDIATE`, restores on guard/DB/zero-row failure, and only trashes after commit. Permanent dedup deletion was already correctly routed through batch permanent delete. Schema/fixture verification confirms tag, collection, face, and dedup membership cascades; generic file deletion also repairs stale person-cover references. Dedup delete tests: 3 passed; full Rust suite: 152 passed / 3 ignored; frontend build passes — `patterns/audit-dedup-delete.md`, `patterns/change-dedup-scan.md`.
- **Motion Photo cache hardening (2026-07-30):** cache hits validate exact expected length plus ISO-BMFF `ftyp`; corrupt/truncated entries rebuild, invalid extracts are rejected, and concurrent same-key writes use unique temp files. Cleanup touches active hits, protects them for 15 minutes, removes hour-old temp leftovers, and only prunes namespaced motion entries. Legacy system-temp purge is a one-time migration. XMP/cache tests: 11 passed; full Rust suite: 149 passed / 3 ignored; frontend build passes — `patterns/audit-xmp-motion-cache.md`, `patterns/change-live-photo.md`.
- **Dedup SQLite concurrency fix (2026-07-29):** exact BLAKE3 and similar dHash scans compute outside write transactions and flush 256-row cache batches; similar O(n²) clustering also moved outside the atomic group replacement transaction. Dedup now uses the shared WAL/5s-timeout connection pool, and a race-free RAII gate prevents overlap with full album indexing; rejected index queues pause cleanly in the UI. Full Rust suite: 141 passed / 3 ignored; frontend build passes — `patterns/change-dedup-scan.md`.
- **Motion Photo one-line XMP fix (2026-07-29):** numeric XMP extraction now uses `quick-xml` local-name matching for both elements and attributes, fixing complete-header Motion Photo detection on minified packets and rejecting similar tag names. Full Rust suite: 138 passed / 3 ignored — `patterns/change-live-photo.md`.
- **Destructive file-operation consistency (2026-07-29):** trash and permanent file/folder delete now validate current-library IDs/paths, stage media with a same-directory rename, and restore it on DB failure or zero-row deletion. Trash restores the original name before invoking the system recycle bin; permanent delete finalizes after DB commit, and batch delete rejects duplicate targets, verifies deleted-row counts, and reports only completed paths. Replace-move rollback and injected DB-failure restoration have regression coverage — `patterns/change-destructive-file-ops.md`.
- **Black-hole rAF lifecycle cleanup (2026-07-29):** `PhotoVortexLayer` now tracks/cancels pending one-shot capture frames across rapid active changes and unmount; Home starts the growth timer only while `gravityActive`, eliminating the theme-off 60fps no-op loop — `patterns/change-black-hole-theme.md`.
- **Linux release resource fix (2026-07-29):** the packaged photo-frame fallback `src-tauri/resources/branding/default-frame-logo.png` is now explicitly tracked despite the generated-resource ignore rules. This fixes clean GitHub Actions checkouts failing Tauri branding resource expansion after libheif completed — `patterns/release-build.md`.
- **GitHub README refresh (2026-07-29):** English/Chinese repository introductions now describe the actual v1.1.0 library, local-AI/plugin, built-in creative, Live/Motion, theme, performance, and Windows/Linux feature set. The README screenshot is `docs/public/screenshots/picaipic-v1.1-smart-tags.png` (2560x1380 source capture).
- **Release dead-code warning cleanup (2026-07-29):** EXIF binary fallback wrapper is test-only; obsolete pre-state-cache scan wrappers and unused public XMP extraction wrappers were removed. Active profiled scan/XMP paths are unchanged, and Rust format/check pass without the reported warnings.
- **Windows/Linux-only platform scope (2026-07-29):** removed committed Android/iOS icon trees plus macOS `Info.plist`, ICNS, and Objective-C pasteboard bridge; Tauri bundle/capability/Cargo and FFmpeg docs now declare Windows/Linux only. Upstream lockfile platform metadata and vendored third-party scripts remain intact. Icon regeneration syncs only root Windows/Linux/shared assets — `patterns/release-build.md`.
- **Application icon refresh (2026-07-29):** repo-root `favicon1.ico` now uses the supplied PicAiPic network-cat mark; Windows EXE/taskbar/tile and shared app/frontend assets are refreshed. Regeneration uses a temporary directory plus hash-sync, and Windows packaging excludes Android/iOS/macOS outputs to avoid WebView user-mapped-file locks. Windows packages require a clean rebuild to embed the new icon — `patterns/release-build.md`.
- **Plugin menu lifecycle visibility (2026-07-29):** manifest-contributed image toolbar/context-menu actions (including SA-LUT) are exposed only for `reachable && managed` plugins. Settings broadcasts status changes across webviews so start/stop/restart updates main/viewer menus immediately. Frontend build passes — `context/plugin-runtime.md`, `patterns/change-ai-plugin.md`.
- **Large-grid scroll smoothness (2026-07-29):** viewport generations stop stale thumbnail queues after scrollbar jumps; overlapping metadata chunks share/await one promise, and thumbnail IPC is deduplicated by request + viewport + file id. Virtual items use contained `translate3d` positioning. Thumbnail cards lazy-mount context menus, observe size only for responsive 90°/270° rotations, and no longer issue per-card plugin loads or leave transition timers. Frontend production build passes — `patterns/change-library-perf.md`.
- **Supplement audit re-verify + hygiene (2026-07-26):** `docs/review/code-review-2026-07-26-supplement.md` revised (status table: true debt vs overstated/stale). S8: shared `t_common::lock_mutex` poison recovery across main/t_sqlite AI/t_face/t_dedup/t_utils/t_cmds. P5: stripped production debug `console.log`; failures use `console.error`. Skipped low-ROI S10 face_indices Arc and already-done P1 decode-out-of-lock. S6: `t_sqlite::query_builder_tests` (14) for search/smart-rule SQL shape.
- **FX themes (2026-07-26, feature branch):** menu **Default / Retro / CMYK / Black hole / Cyberpunk** (`THEME_ID` 0–4). Both FX themes dual-pin + `data-theme=dark`, share `dynamicThemeIntensity`, maximize + **6s** idle (seven-way Home gates). GridView **theme-gates** WebGL layer mount (one context at a time).
  - **Black hole:** `BlackHoleBackground` cosmos; idle → GPU-limit-aware `PhotoVortexLayer` UV absorb; WebGL/capture failure → `useGravityWarp` CSS spiral. Design `docs/guide/black-hole-idle-theme-design.md`; pattern `patterns/change-black-hole-theme.md`.
  - **Cyberpunk:** always-on `CyberpunkBackground` (night-city + **sprite-baked** rain/particles/kana); idle → GPU-limit-aware `PhotoGlitchLayer` with **mediump-safe** hash + `mod(time)`; failure → CSS translation/color/RGB-edge glitch; intensity `>0`. Spec `docs/superpowers/specs/2026-07-26-cyberpunk-idle-glitch-design.md`; pattern `patterns/change-cyberpunk-theme.md`.
- **FX correctness/perf (2026-07-26):** mediump-safe glitch rand; ambient sprite bake; no per-frame reflow in vortex/glitch paint; theme-gated GL mount (`130b33a`). Polish: ambient `seedField(false)` on resize (no rain reseed jump); glitch tracks/cancels capture rAF (`1aa0a59`).
- **Audit harden (2026-07-26):** import_url timeout/size; copy orphan cleanup; restore atomic write; plugin path_inside; embed warm; lock poison recovery; scan dedupe. Report `docs/review/code-review-2026-07-26.md`.
- **S1/S6 SQLite audit follow-up (2026-07-27):** `AFile::new` delegates header/EXIF/orientation/identity/descriptions/capture extraction and RAW metadata merge policy to focused helpers; CRUD retains connection-backed cores for temporary SQLite coverage. Fixtures cover TIFF fields plus pure `RawMeta` overlay behavior. Stop at this orchestration boundary: Live/Motion/HEIC, geocode, AI prompt and final assembly stay in `new`. Validation: `cargo check`; full `cargo test` **107 passed / 3 ignored** — `patterns/test-sqlite-crud-fixture.md`, `docs/review/code-review-2026-07-26-supplement.md`.
- **Scan profiling (2026-07-27/28):** `D:\100k` all-JPEG micro-benchmarks remain 43.7 files/sec for `AFile::new` and 22.0 files/sec for serial temp-DB + 200px thumbnails. The active `D:\MultiModalKaggleDataset` comparison set has 10,343 files (10,340 JPEG + 3 PNG; 10,150 successful thumbnails).
- **AI prompt JPEG I/O optimization (2026-07-28):** `t_ai_prompt::extract_jpeg_prompt` reuses the pre-read JPEG header through SOS/EOI, avoids reopening complete JPEGs for COM metadata, and consumes EXIF `UserComment` already parsed by `AFile::new`; incomplete headers still use file fallback. Regression coverage includes complete, truncated, COM, and EXIF cases.
- **DirectML embedding probe (2026-07-28):** the bundled `ort` runtime's DirectML path caused a native `STATUS_ACCESS_VIOLATION` during model loading on the test machine, including while querying provider availability. The in-process probe is disabled; keep normal CPU sessions until a separately packaged DML runtime/subprocess probe is available.
- **CPU embedding tuning (2026-07-28):** `PICAIPIC_AI_INTRA_THREADS` optionally controls ONNX intra-op threads (1-64), default remains 2. On the active dataset, 4/8 threads were slower overall than the lower-thread baseline; CPU utilisation alone is not a tuning target.
- **Misnamed image fallback (2026-07-28):** embedding decode now guesses format from file content after the JPEG fast path; `.jpg` files containing RIFF/WebP are no longer forced through the JPEG decoder.
- **CPU batch embedding (2026-07-28):** vision ONNX accepts dynamic batches; the scan worker groups up to 8 decoded images and commits successful 512-d vectors in one SQLite transaction (one matrix-cache invalidation). Batch preparation, inference, or transaction failures fall back to single-file generation. The 1024px source cap is retained. On `MultiModalKaggleDataset`, transaction batching completed in 1,716.720s with 10,150/10,150 embeddings and thumbnails, 19.1% faster than the pre-transaction batch run (2,121.0s) and 28.8% faster than the 4-thread pre-batch run (2,410.6s).
- **Embedding split profiling (2026-07-28):** opt-in scan profiling records configured capacity, batch count/requested/prepared items plus prepare (lookup+decode), engine (preprocess+ONNX), transaction write, and single-file fallback timing once per batch. A warm-source diagnostic run split 1,025.576s into 230.476s prepare, 776.918s engine, 2.247s write, and zero fallback, so engine work is next. `PICAIPIC_EMBED_BATCH_SIZE` accepts 1-32 for controlled probes; default stays 8.
- **Embedding batch-16 + preprocessing follow-up (2026-07-28):** batch 16 completed in 2,173.659s with 10,150 successes but overlapped a 100k matrix/search and unfinished ANN build; it failed the product performance gate and default stays 8. CLIP batch preprocessing now consumes owned images without cloning each decoded source and writes directly into contiguous NCHW storage; shape/channel regression coverage added.
- **100k ANN CPU fix (2026-07-28):** app open warms the exact embedding matrix only and ANN is default-off (`ann=disabled`); 110k exact searches already return responsively. `PICAIPIC_EMBED_ANN=1` explicitly enables lazy first-search HNSW in a dedicated 2-thread Rayon pool (`PICAIPIC_EMBED_ANN_BUILD_THREADS` accepts 1-8). This keeps 100k libraries supported without a long CPU build on each process lifetime.
- **Embedding warm-up meaning (2026-07-28):** `embed_matrix warm ready ... n=100000 dim=512` rebuilds the process-local search matrix from SQLite at app start; it does not re-run image decode or ONNX embedding. Disk persistence is future work.
- **110k search verification + warm profiling (2026-07-28):** 110,343 vectors start with `ann=disabled`; repeated `bird` queries use exact `matrix=1` and return 30 stable results without scheduling HNSW. Default warm logs total time + allocated matrix MiB. `PICAIPIC_EMBED_WARM_PROFILE=1` adds SQLite/BLOB vs vector/norm timing, working set, then a five-second process CPU sample after five seconds of settle. Restart the existing library, leave ANN unset, and do not search during the sample; never reimport for this check. Build a versioned atomic disk matrix cache only if measurements justify it.
- **110k warm result + allocation follow-up (2026-07-28):** first profile = 3.150s total (SQLite 1.217s, vector/norm 1.912s), post-settle CPU 0.06%; persistent 90% CPU is closed and disk cache is deferred. `COUNT + exact reserve` saved 40.7 MiB but regressed warm to 5.846s and was rejected.
- **Matrix load copy removal (2026-07-28):** accepted warm path uses one SQLite scan, borrowed BLOB parsing instead of ~215.5 MiB cumulative temporary copies, then one final vector shrink. It replaced the rejected double-scan exact-reservation path and now feeds the 2.876s / 216.8 MiB result.
- **Matrix warm final result (2026-07-28):** final 110,343-vector profile = 2.876s total (SQLite 1.062s, build 1.804s), 216.8 MiB matrix, and 0.00% post-settle CPU. This is 8.7% faster than baseline and saves 40.7 MiB. Defer disk cache; next split scan engine into preprocessing vs ONNX inference before further throughput changes.
- **Embedding engine split (2026-07-28):** `[scan-profile]` retains aggregate engine seconds and adds preprocessing, ONNX inference, and residual overhead. The 687.636s full run supplied the decision baseline; no further reimport is required solely to populate the split.
- **Embedding one-batch prefetch (2026-07-28):** the 687.636s baseline split 10,150 embeddings into 239.678s source preparation, 130.617s NCHW preprocessing, 299.305s ONNX inference, and 1.937s write. The worker now passes path/type/orientation directly and preprocesses one bounded next batch outside the AI mutex. Real heterogeneous `D:\Desktop` validation passed 1,255/1,255 embeddings, fallback 0, and 36 prefetched batches. The homogeneous 1,000-file follow-up finished in 47.788s with 970/970 embeddings, fallback 0, and 80/123 prefetched batches (7.89 items/batch); cumulative prepare + engine + write was 65.071s, confirming overlap without claiming an unmeasured A/B speedup.
- **Synchronous index split (2026-07-28):** opt-in scan profiling now splits `index_single_file` into folder/fetch/stat/metadata/refresh/write/refetch/assemble/other. `PICAIPIC_SCAN_SLOW_FILE_MS` accepts 1-600000 and emits `[scan-slow-index]` only for threshold outliers; normal scans do not take the per-stage clocks. Capture the next natural import or changed-file rescan instead of deleting/reimporting an album solely for profiling.
- **Warm index rescan + folder cache (2026-07-29):** the 1,000-file prefetch sample / `D:\Desktop` warm rescans initially completed in 2.022s / 2.643s with zero thumbnail/embedding tasks. The scan worker now caches `folder path -> folder id` for one scan only. A matched 1,000-file rerun was 1.677s (-17.1%) and index was 1.059s (-25.3%), with 985 hits/15 misses and folder time 0.008s vs 0.380s. Validate `D:\Desktop` next; only then evaluate SQLite fetch/timestamp-write batching.
- **Bounded seen writes accepted (2026-07-29):** unchanged `last_scan_time` writes batch before matching progress checkpoints. On the matched 10,343-file warm rerun, 500-item windows reduced transactions 207 -> 21, write time 1.130s -> 0.605s (-46.5%), and total 10.164s -> 8.786s (-13.6%). A successful seen write still precedes checkpoint; recovery redoes at most 499 files. Retain per-file stat (1.311s) for correct change detection rather than introduce a complex weaker-consistency path.
- **Lightweight scan-file cache accepted (2026-07-29):** one-scan, one-album state preload is keyed by folder id and basename and retains no full metadata or embeddings. `D:\Desktop` accepted it on 1,293 unchanged files: preload 0.014s, 1,293 hits / 0 misses, fetch 0.000s, total 2.016s -> 1.068s (-47.0%), and index 0.954s -> 0.182s (-80.9%). Misses, changed rows, missing thumbs, and preload failures retain the established lookup/update path. Warm stat at 0.153s is next only if representative profiling justifies its complexity.
- **Cold metadata diagnosis (2026-07-29):** a copied 1,000-file initial import took 44.662s; synchronous index was 39.991s and new-file metadata 37.379s. Cumulative embedding engine work (40.972s) overlapped traversal, so leave batch 8/CPU threads/ANN unchanged. With phase profiling enabled, `AFile::new` now records file-info/inode, header, dimensions, EXIF, extraction/fallbacks, Motion/HEIC, geocode, prompt, and assembly subphases. Use a new copied sample to choose one safe cold-path optimization; never recreate an existing album for this measurement.
- **Cold binary EXIF fallback (2026-07-29):** matching 1,000-file cold run took 45.172s and isolated binary fallback at 17.172s (EXIF parse 7.998s; Motion XMP 4.731s). It repeatedly scanned the same 128 KiB header once per fallback tag. The code now makes one tolerant pass to collect requested fields, preserving full EXIF precedence plus Sony orientation and Apple Live Photo ContentIdentifier fallback. Validate with a new copied sample before moving to Motion XMP.
- **Cold binary validation + Motion header reuse (2026-07-29):** binary fallback fell 17.172s -> 2.372s (86.2%), index 40.338s -> 24.591s, with 970/970 thumbnail/embedding successes; producer speed exposed a 16.693s drain tail and total reached 43.387s. Motion detection now reuses a JPEG header only when a complete marker walk reaches SOS/EOI; incomplete JPEGs and HEIC retain the 512 KiB file-read fallback. Validate the new copied-sample result before further EXIF or concurrency changes.
- **Cold drain pipeline experiment rejected (2026-07-29):** Motion validation reduced Motion XMP 4.989s -> 3.067s and index to 22.618s, but drain was 18.967s. A fill-during-inference experiment raised prefetch coverage to 121/122 batches but regressed the next cold import to 100.724s, 73.209s drain, and 962/970 task replies. Waiting for a full open batch starved tail replies into the 60s timeout. A 0.758s warm rescan then had zero thumbnail/embedding tasks, confirming the timed-out requests had still persisted their vectors. The experiment is reverted to prior safe one-batch opportunistic prefetch; do not retry it.
- **Cold drain split diagnostics (2026-07-29):** profile mode now records embedding-permit wait, channel-send wait, worker-reply wait, and each timeout class separately. It does not alter queueing, batching, or concurrency. Run one fresh copied cold import and choose the next optimization only from the dominant new field.
- **In-flight embedding permit probe accepted (2026-07-29):** the 46.360s cold profile showed 10,088.257s cumulative permit wait, zero send/reply timeouts, and 19.902s drain. The 3-batch probe completed 970/970 with no timeout, reduced permit wait to 5,816.884s, drain to 13.601s (-31.7%), and total to 41.239s (-11.0%), despite 1.173s slower indexing. `PICAIPIC_EMBED_INFLIGHT_BATCHES` accepts 2-4 and now defaults to 3; it admits a bounded queued third batch without changing the single ONNX session, batch size, or worker tail behavior.
- **Cold EXIF split diagnostics (2026-07-29):** scan profiling retains aggregate EXIF time and now separates the 128 KiB header parse from complete-file fallback, with attempt counts for both. Run one fresh copied cold import before optimizing either path.
- **Cold EXIF fallback accepted (2026-07-29):** a 1,000-file profile split EXIF into 4.595s header parsing and 5.012s across 785 whole-JPEG fallback attempts. All 995 JPEG headers reached SOS/EOI; 780 had no EXIF APP1. Complete headers without EXIF now skip the provably empty file scan, while incomplete/non-JPEG/EXIF-present headers retain it. Validation: fallback 785 -> 5 and 5.012s -> 0.044s; EXIF 9.611s -> 4.563s; index 25.881s -> 19.694s; 970/970 derived-media successes.
- **Cold EXIF header internal split (2026-07-29):** profile-only instrumentation now separates the permissive 128 KiB reader into container parsing, `Exif\0\0`/TIFF signature scans, and raw `to_vec` + reader parsing. It keeps the exact recovery order and normal scans stay free of these clocks. Run one fresh copied cold import before considering a no-EXIF JPEG marker fast path; do not tune embedding drain from its variable tail.
- **Cold no-EXIF JPEG marker fast path accepted (2026-07-29):** the internal split measured 4.420s in signature scans versus 0.110s container parsing and zero raw parses. Complete JPEG headers without EXIF APP1 now bypass the permissive reader; incomplete and EXIF-bearing headers retain recovery. A matched 1,000-file import reduced EXIF header 4.532s -> 0.048s, metadata 16.753s -> 12.979s, and index 19.841s -> 16.170s, with 970/970 derived-media successes. Total varied 38.064s -> 39.341s because drain rose 15.953s -> 20.921s; do not attribute that variation to EXIF.
- **Cold EXIF orientation fallback accepted (2026-07-29):** complete JPEG headers already proven to lack EXIF now retain default orientation 1 instead of another binary 128 KiB scan. On the matched 1,000-file cold import, orientation extraction fell 4.086s -> 0.842s, metadata 12.650s -> 9.884s, and index 16.004s -> 13.293s, with 970/970 derived-media successes.
- **Cold Motion XMP marker fast path accepted (2026-07-29):** internal timing isolated 3.407s of 3.502s Motion work in 1,000 generic header XMP scans. A complete JPEG marker walk now skips that scanner only when APP1 has neither the XMP namespace nor `<x:xmpmeta>`; candidate, incomplete, and HEIC paths preserve tolerant parsing/fallback. Validation reduced Motion 3.502s -> 0.149s, metadata 9.953s -> 6.650s, and index 13.276s -> 10.069s, with 970/970 successes.
- **Cold binary TIFF fallback marker fast path accepted (2026-07-29):** profile found 1.965s TIFF-signature scanning across 1,000 headers, but zero TIFF bases in the complete no-EXIF-JPEG group. That group now skips the impossible binary fallback while EXIF-bearing, incomplete, and non-JPEG headers retain it. Validation reduced binary fallback 2.931s -> 1.004s, metadata 6.689s -> 4.347s, and index 10.024s -> 7.197s, with 970/970 successes. Drain varied to 28.296s and remains separate.
- **Flow Launcher scan comparison (2026-07-28):** Flow's fast file search queries Everything's persistent NTFS index or Windows Search with a default 100-result cap; it does not decode media, create thumbnails, parse EXIF/RAW, or run CLIP. PicAiPic already uses folder-mtime dirty detection for startup deltas. Do not add a required Everything/Windows Search dependency or expect it to improve initial-import inference; use the current preprocess/inference split to choose the next optimization.
- **Scan preview + RAW thumbs (2026-07-26):** always advance `processed` on thumb fail; RAW embedded JPEG first — `patterns/fix-library-scan-selection.md`.
- Tauri 2/Rust desktop host with Vue 3 frontend for Windows and Linux.
- **v1.1.0** app/docs versions aligned; tag `v1.1.0` has a **private draft** multi-arch release (Linux deb/AppImage + Windows x64/arm64 MSI + updater latest JSON on the Release assets). Not published; keep private until the owner decides.
- Release CI publishes installers to **GitHub Release assets** (not Actions artifact storage) after quota failures; PR builds use best-effort artifact upload — see `patterns/release-build.md`.
- Folder-first multi-library browsing, SQLite metadata (schema v6+; collections v7, unique afiles v8), indexing/recovery, thumbnails, timeline/folder/location/camera/lens/tag/favorite/rating/face filters, deduplication, image editing, and broad image/RAW/video support.
- Rename/move disk↔DB consistency: `rename_file` / `rename_folder` roll back disk on DB failure (aligned with `move_file`); `edit_album` propagates name-column errors; dedup `get_files_by_sizes` reuses precomputed suspicious sizes via chunked `IN` binds.
- Local AI search and face processing use bundled ONNX models; FFmpeg is bundled as a sidecar for video workflows.
- AI plugin host: discovery, signed package install/trust, permissions, install profiles, shared/private/external Python runtimes, lifecycle, async tasks, output adopt/discard, runtime-conflict detection, two sample plugins.
- Plugin security A+B+C: bearer token, Ed25519 package signing/trust, default input-file staging; Windows deny-ACL opt-in only.
- Sandbox **Phase 0–2 done**: cross-platform staging, fail-closed, diagnostics, `plugin_writable_roots`, same-volume hardlink→copy. Phase 3–4 (network OS / Landlock) **not** implemented — `docs/ai-plugin-sandbox-roadmap.md`.
- Live Photo / Motion Photo: detect/pair/preview/export; HEIC-internal video; keyframe overwrite (JPEG); album rescan; user guide `docs/guide/live-photo.md`.
- Confirmed shared→plugin-private runtime switch + managed model open/validate/import (Settings).
- Merged to main (2026-07-18): Live Photo polish (#1), sandbox Phase 0–2 + runtime/model UX (#2).
- Built-in **Phase A crop presets** (2026-07-18): ImageEditor ratios + photo-size catalog + custom favorites — `photoSizePresets.ts`, `patterns/change-crop-presets.md`.
- Built-in **Phase B collage** (refined 2026-07-19): multi-select 拼图 — equal + magazine freeform cells (ids 2, 2v, 3a, 3b, 4, 4m, 6, 6m, 9; NeoImaging-style), strip, free canvas + free drafts; host `export_collage` with `template: "cells"` + cell-sized source downscale — `patterns/change-collage.md`.
- Built-in **Phase C1–C2 batch** (2026-07-18): multi-select 批处理 wizard — composable actions including border/expand/watermark/text (+ optional EXIF capture-time stamp); templates; progress/cancel; host `batch_process_images` — `patterns/change-batch-process.md`.
- Dialog safety (2026-07-19): collage draft save/delete + batch template save/delete/overwrite use `MessageBox` / plugin-dialog `ask` only — no `window.prompt`/`window.confirm` (WebView no-op risk). Free collage rotate source headroom uses true AABB.
- Batch process **parallel workers** (2026-07-19): serial dest planning + JoinSet concurrency (2–8); GridView VirtualScroll buffer 4→8. SearchBox submits on Enter only (no per-key SQLite). Face index **CPU parallel** (2026-07-19): 2–4 worker engines + batched SQLite writes; GPU EP still future.
- **Smart Albums / 智能相册** (2026-07-19): rule SQL + sidebar list/editor + Content smart source — `patterns/change-smart-albums.md`. Inserted at absolute sidebar index 1 (`SIDEBAR.SMART`); later panels shifted — always use `SIDEBAR` constants.
- **Smart Albums size/error UX (2026-07-24):** backend size supports is_not/empty/not_empty; size always MB→bytes; person/camera/lens pickers in SmartAlbumEdit; Content toasts query errors instead of silent empty list — `patterns/change-smart-albums.md`.
- **Smart Albums sort + local-day dates (2026-07-24):** SmartAlbumEdit sort type/order; date before/after/between use local calendar-day compare (same as calendar content) — `patterns/change-smart-albums.md`.
- **Smart Albums empty loading + editor UX (2026-07-24):** SIDEBAR.SMART empty/stale selection no longer infinite-spins; date default in_last; empty pickers guided — `patterns/change-smart-albums.md`.
- **Collections / 集合** (2026-07-19): SQLite membership + left-sidebar tray + drag-add + Content `collection` query source — `patterns/change-collections.md`.
- Large-library **search perf** (2026-07-19): `cosine_similarity_blob` + chunked `get_files_by_ids`; semantic search defers bulk thumbs to viewport — `patterns/change-library-perf.md`.
- **Library polish Phase 5** (2026-07-19): Library sidebar All/Favorites/On this day quick entries + Content view-adaptive date grouping (`effectiveDateGrouping` → GridView) — `patterns/change-library-shortcuts.md`.
- **Lap 0.3 port (core + UX, 2026-07-19)**: program order perf → 4-pane → collections → smart albums → library polish **done**; bugfix wave (`patterns/fix-library-scan-selection.md`); then UX pack:
  - **AI PNG/JPEG prompt → empty comments**: scan A1111/NovelAI/InvokeAI/ComfyUI PNG text + JPEG UserComment/COM; default on; no full-library backfill — `t_ai_prompt.rs`, `patterns/change-ai-prompt-import.md`
  - **Thumbnail media badges**: format/ISO/shutter/aperture/focal/exposure overlays (default off) — `settings.grid.mediaBadges`, `patterns/change-media-badges.md`
  - **Viewer background modes**: theme/black/white/gray/checker + shortcut **B** — `mediaViewer.backgroundMode`, `patterns/change-viewer-background.md`
  - **AI search file-type + groups**: `ImageSearchParams.search_file_type`; Visual/Similar/Filename section headers — `patterns/change-ai-search-filters.md`
- Built-in **photo print layout / 冲印排版** (refined 2026-07-19): fill-the-paper packing + A4 presets + custom paper form + optional library import; **export** = full plan DPI; **print** = print-sized sheet (long edge ~1800px) + blob URL + `window.print` (fast dialog open); host downscales sources to cell need; print-cache purge + 24h stale temp cleanup — `printLayout.ts`, `PrintLayoutDialog.vue`, `export_print_layout` — `patterns/change-print-layout.md`.
- Dialog open freeze fixed (2026-07-18/19): never mutate pinia `printLayout` inside `computed`.
- **Batch optional library import / G2** (2026-07-20): host `BatchProcessResult.outputPaths`; wizard `batchProcess.importToLibrary`; saveAs → sequential `importFile`; overwrite → `updateFileInfo` only — `patterns/change-batch-process.md`.
- **Signing multi-key + local revoke / G6** (2026-07-20): registry `keys[]` + `revokedKeys`; `trust_publisher` adds keys; `revoke_publisher_key` / `list_revoked_keys`; install fails closed on revoked keys — `docs/ai-plugin-security-hardening.md` Q3 closed.
- **Sandbox Phase 3–5** (2026-07-20): **Phase 3** opt-in Windows netsh outbound + policy env. **Phase 4** opt-in Linux Landlock (ABI probe + path rules + pre_exec; soft-fail). **Phase 5** env hygiene opt-in. Default confinement remains Phase 0–2 — `docs/ai-plugin-sandbox-roadmap.md`, `t_sandbox.rs`.

- **G10 FileInfo Live hover** (2026-07-20): info-panel preview hover/long-press plays Live/Motion; i18n labels — `FileInfo.vue`, `patterns/change-live-photo.md`.
- **G11 print magazine pack** (2026-07-20): free-rect `magazine` strategy + auto scoring — `printLayout.ts`.
- **G12 export-only DPI** (2026-07-20): DPI under Export options; Export DPI copy — `PrintLayoutDialog.vue`.
- **Photo style / 照片格调 + LUT library** (2026-07-21): presets+manual merge; geometry-aware host preview (flip/rotate/crop); decode/JPEG caches; combined color-match+style; crop-aligned compare in editor — `photoStylePresets.ts`, `t_lut.rs`, `t_image.rs`, `patterns/change-photo-style.md`.
- **Traditional color match / 追色 + style LUT** (2026-07-20/21; **perf pack 2026-07-22**): host global Lab stats match (no segmentation); ImageEditor **调色→追色** + batch `colorMatch`; preview `color_match_preview`; **single-image style 33³ `.cube`** via `export_color_match_lut` (reference preferred, else current; not dual-image match map; not G7 SA-LUT). Stats downsample both images to 1024 max-edge; single-pass full-res grade (no multi-plane 50MP buffers); Lab a/b full 0–255; LUT size 17–65 errors instead of silent clamp — `t_color_match.rs`, `patterns/change-color-match.md`.
- **Batch cancel/crash atomic write (2026-07-30):** UUID sidecar + synced exact-path journal then rename; guard drop cleans cancel/error/task abort and startup cleans a process-crash leftover; progress `current` clamped to `total` — `t_output_temp.rs`, `t_image.rs`, `patterns/change-batch-process.md`.
- **Audit follow-ups (2026-07-22):** SQLite pool path normalize + max 8 idle; `update_column` allow-lists; face cancel progress→100% + safer model Err; plugin setup log ring 2000 lines — `t_sqlite.rs`, `t_face.rs`, `t_plugin.rs`.
- **Collage/cluster/watermark audit pack (2026-07-22):** collage atomic temp+rename; JPEG turbo scale-on-decode in `load_image_for_layout`; strip max 48; cluster linear top-k + cancel `Err`; watermark ISO datetime + batch watermark/time caches — `t_image.rs`, `t_cluster.rs`, `t_face.rs`.
- **Multi-image compare library entry** (2026-07-21): context menu "Compare with next / selected" → `forceSplit` 2-up viewer — `Content.vue`, `fileMenu.ts`, `patterns/change-compare-viewer.md`.
- **ImageViewer Edit toolbar** (2026-07-21): built-in Edit button; `image.toolbar` plugin buttons no longer rendered (plugins remain on context menu) — `MediaViewer.vue`, `ImageViewer.vue`.
- **Batch capture-time watermark** (2026-07-21): text/image watermark optional EXIF time stamp — `batchProcess.ts`, `BatchProcessDialog.vue`, `t_image.rs`, `patterns/change-batch-process.md`.
- **Photo frame / 相框 G-Frame-1+G2** (2026-07-22; **bug pack + presets same day**): classic white/black + float/sink blur+shadow; host `photo_frame_preview` / `export_photo_frame`; dialog + optional library import. Custom presets in `config.photoFrame.presets`; **frame** default logo = `src-tauri/resources/branding/default-frame-logo.png` (`logo-pic.png` wordmark). **App chrome icons** = network-cat from **`favicon1.ico`** → Windows/Linux/shared icon assets (not frame logo). Package: `build-exe.bat` passes `-Clean`; `package_windows.ps1` regenerates icons then `cargo clean -p PicAiPic`. — `photoFrameTemplates.ts`, `PhotoFrameDialog.vue`, `t_image.rs`, `patterns/change-photo-frame.md`, `scripts/regenerate_app_icons.ps1`.
- **Face cluster ANN pack (2026-07-22):** P0 logs + exact/blocked/ANN adaptive + `face.clusterMode`; `instant-distance` HNSW; P3 deferred — `docs/guide/face-cluster-ann-plan.md`, `patterns/change-face-index.md`.
- **LIVE filter bit 8 + AI threshold honor (2026-07-22):** toolbar LIVE; no force 0.25 on text search.
- **CLIP quality + scan + search (2026-07-23; ranking fix 2026-07-24):** **Text path:** abs floor primary `max(0.16, thr*0.85)`; rel top1*0.85 empty-fallback only; thr_cap VH30/H40/M50/L200 + **user limit hard cap**; free-text template; UI thr ladder **[0.28,0.24,0.20,0.16]**; **smart tags follow slider**. **Similar-from-file (image→image, same day):** separate floors **0.88/0.82/0.74/0.62**, thr_cap **12/24/40/100**, exclude query self; Settings thr/limit re-runs active similar/search. Embed: thumb/embed decouple, edge 1024, RAW LibRaw preview, semaphore 1. Patterns: `patterns/change-ai-search-filters.md`, `patterns/change-smart-tags.md`.
- **Smart tags 6-bucket + thr (2026-07-24):** people/pets/landscape/architecture/plants/birds; people=`a photo of people`, pets=common species list; default `thresholdIndex=1` (High, new installs); Settings thr re-runs smart-tag CLIP path directly + numeric coerce; not a detector (face index for identities) — `patterns/change-smart-tags.md`, `patterns/change-ai-search-filters.md`.
	- **Track C product C (2026-07-24):** bundled **bilingual int8** text tower as `src-tauri/resources/models/text_model.onnx` (EN CLIP text removed from install); **no Settings model switch**; optional cloud re-download for observation; self-host `picaipic-binaries` tag `models`. Vision stays CLIP B/32 (**no reindex**). Guide `docs/guide/altclip-phase0-probe.md`. Pattern `patterns/change-image-search-model.md`.
	- **AI search stop-bleed + matrix + ANN (2026-07-24):** legacy sentence multilingual space mismatch fixed by product C (aligned pack). **In-memory embed matrix** + rayon; optional background HNSW; SQL BLOB fallback; invalidate on write/clear/library switch. Face cosine dim fail-closed. Dedup Similar dHash (schema v9). Plan: `docs/superpowers/plans/2026-07-24-ai-search-stopbleed-and-embed-cache.md` + ANN/pHash design. Patterns: `patterns/change-image-search-model.md`, `patterns/change-library-perf.md`.
	- **Image-search model tracks (2026-07-23; product C 2026-07-24):** **A stop-bleed shipped**. **B0 abandoned.** **C bilingual text shipped as default**. Vision default = **CLIP B/32**. **Track B Phase 0:** SigLIP2 quant loads (Rust int8 fail). Real ~96 compare: **CLIP 昆虫/植物弱；SigLIP2 小鸟(小主体)也会当昆虫** — **no clear quality win; no product UI on this pack alone**. Scripts: `scripts/probe_siglip2_onnx.py` + `scripts/compare_clip_vs_siglip2.py`; guide `docs/guide/siglip2-phase0-probe.md`; pattern `patterns/change-image-search-model.md`.
- **Settings hydrate gate + mediaBadges equal-noop (2026-07-22):** `patterns/settings-cross-window-sync.md`; import/updateFileInfo rethrow.
- **G13 system print UX** (2026-07-20): hint that printer/tray is system dialog only; no host `print_file`.
- **Correctness fixes (2026-07-20):**
  - Face clustering is **incremental**: preserves existing person names/assignments; only unassigned faces join clusters or create new `Person N` — `t_cluster.rs`, `Face::get_all_for_clustering` includes `person_id`.
  - Face detector naming/IO aligned to **SCRFD det_500m** with output/anchor mismatch fail-closed — `t_face.rs`, `t_common.rs`.
  - DB storage move/reset clears conn pool after migrate — `t_cmds.rs` (`change_db_storage_dir` / `reset_db_storage_dir`).
  - Collage host rejects source count > cell count (no silent drop) — `t_image.rs`.
  - Batch dialog: hue min **-180**, removed `&& false` dead disable — `BatchProcessDialog.vue`.
  - Dedup skips rows without `file.id` instead of `unwrap` panic — `t_dedup.rs`.
  - **Calendar empty list (root cause):** after Smart Albums was inserted at sidebar index 1, `Content.vue` still treated calendar as index `3` (now Search). Calendar is absolute index `4`. Fixed via `SIDEBAR` constants in `constants.ts` and rewired Content/Home routing. Also: local-day SQL filter, month/day number UI on dots, numeric QueryParams coercion.

**Cancelled / deferred (owner 2026-07-20):**
- **G1** collage-as-batch-action (C3 insert) — cancelled.
- **G7** SA-LUT `export-lut` — cancelled / not doing.
- **G8** face index GPU EP — cancelled / not doing.
- **G9** optional whole-library empty-comment AI-prompt backfill — not doing (scan-time empty-only remains).

**Not yet built / future work:**
- **Large-library face clustering** — plan `docs/guide/face-cluster-ann-plan.md`; **P0–P2 + HNSW ANN done** (`instant-distance`, blocked fallback, `face.clusterMode`). **P3 disk/incremental ANN deferred** (low ROI; measure first; prefer embedding cache over graph serde if needed). Ops: `patterns/change-face-index.md`.
- Sandbox deeper enforcement: Linux netns / real WFP; seccomp; Landlock×ROCm matrix; optional cache ref-range (Phase 3–5 flags exist, default off).
- Remote signing CRL / dual-sign key-transition artifacts; recurring release-exe plugin regression after host changes.
- Broader HEIC sequence sample coverage; broader automated coverage outside plugin-host + current Rust unit tests.
- Publish v1.1.0 draft release (owner decision; repo remains private for now).
- Commit remaining G10–G13 + correctness pack + calendar SIDEBAR fix + color match / photo style / photo frame / face-cluster ANN / LIVE filter / settings sync / favicon1 app icons if not yet on `main` tip.
- In-app updater UX polish deferred until public Release (endpoint already configured).

**Known issues / active risks:**
- `api.js` still maps many **query** IPC failures to `null`/`false`/`[]` (empty-vs-error debt). Mutating paths rethrow for rating/favorite/rotate/batch metadata and for `importFile` / `importUrl` / `updateFileInfo`. Settings cross-window: hydrate-gated emits + equal-noop object setters (`patterns/settings-cross-window-sync.md`).
- Adding a new left-sidebar button **shifts absolute indices** — update `SIDEBAR` in `constants.ts` and every `updateContent` / shortcut branch; never hard-code calendar as `3`.
- Packaged-plugin behavior must be checked in the release executable; dev-mode success alone does not prove installer/resource/runtime correctness.
- GitHub Actions **artifact storage quota** can fail uploads even when builds succeed; prefer Release assets for installers; PR upload is best-effort.
- Release Rust builds can fail at local MSVC/CRT link time in native deps (ONNX/LibRaw) even when `cargo check` passes.
- AI plugin compatibility enforces min/max PicAiPic versions and plugin API major.
- Protocol thumbnail/preview resolve against library id in the URL; preserve isolation.
- pnpm is the sole JS package manager; host/frontend/docs versions aligned at **1.1.0**.
- Historical internal identifiers may still use `Lap`; user-visible paths corrected.
- ffprobe ContentIdentifier key name may vary; `first_exist` checks dotted and underscored variants.

## Routing Table

| Task type | Load |
|-----------|------|
| Understand application flow or subsystem boundaries | `context/architecture.md` |
| Work with libraries, versions, native dependencies, or CI | `context/stack.md` |
| Write/review Rust, Vue, IPC, database, or UI code | `context/conventions.md` |
| Make or revisit a design choice | `context/decisions.md` |
| Set up, run, verify, or package | `context/setup.md` |
| Change AI plugin host, manifest, runtime, task, trust, or sandbox | `context/plugin-runtime.md` |
| Change ComfyUI integration (import/conversion, run/batch/cancel, result import) | `patterns/change-comfy-integration.md` + `docs/comfyui-integration-status.md` |
| Change Live Photo / Motion Photo detection, pairing, or preview | `patterns/change-live-photo.md` |
| Change face indexing performance, clustering, or scan DB batching | `patterns/change-face-index.md` |
| Plan large-library face clustering (ANN / blocked KNN, O(n²) removal) | `docs/guide/face-cluster-ann-plan.md` then `patterns/change-face-index.md` |
| Change calendar dots, day/month selection, or empty calendar content | `patterns/change-calendar.md` |
| Change AI PNG prompt import into comments | `patterns/change-ai-prompt-import.md` |
| Change thumbnail media-info badges | `patterns/change-media-badges.md` |
| Main↔settings Pinia emit/listen, hydrate gate, object equal-noop | `patterns/settings-cross-window-sync.md` |
| Regenerate Windows app icons from favicon1.ico | `scripts/regenerate_app_icons.ps1` then `build-exe.bat` / `package_windows.ps1 -Clean` |
| Change move/replace rollback, trash, permanent delete, or batch delete | `patterns/change-destructive-file-ops.md` |
| Change exact/similar dedup scan, hashes, groups, cancellation, or DB contention | `patterns/change-dedup-scan.md` |
| Change image viewer background modes | `patterns/change-viewer-background.md` |
| Change AI search filters, similar-from-file ranking, or result grouping | `patterns/change-ai-search-filters.md` |
| Add/tune CLIP smart tags (prompts; thr follows settings slider) | `patterns/change-smart-tags.md` |
| Image-search model (CLIP B/32 vision + bilingual text default; Track B/C) | `patterns/change-image-search-model.md` + `docs/guide/altclip-phase0-probe.md` + `docs/guide/siglip2-phase0-probe.md` |
| Change traditional color match / 追色 / host style `.cube` | `patterns/change-color-match.md` |
| Change black hole theme, cosmos, idle vortex, chrome glass, intensity | `patterns/change-black-hole-theme.md` then `docs/guide/black-hole-idle-theme-design.md` and `docs/guide/fx-theme-runtime-compatibility.md` |
| Change cyberpunk theme, night-city ambient, idle photo glitch, intensity | `patterns/change-cyberpunk-theme.md` then `docs/superpowers/specs/2026-07-26-cyberpunk-idle-glitch-design.md` and `docs/guide/fx-theme-runtime-compatibility.md` |
| Scan stuck on preview progress, RAW thumb slow, concurrent scan dups | `patterns/fix-library-scan-selection.md` |
| Plan or implement built-in crop presets, collage, batch, print layout, color match / style LUT, photo style, or photo frame / 相框 | `docs/guide/builtin-tools-roadmap.md` then `patterns/change-crop-presets.md` / `patterns/change-collage.md` / `patterns/change-batch-process.md` / `patterns/change-print-layout.md` / `patterns/change-color-match.md` / `patterns/change-photo-style.md` / `patterns/change-photo-frame.md` |
| Change EXIF photo frame / 相框 (classic, float/sink blur, logo) | `patterns/change-photo-frame.md` + roadmap Phase G |
| Build/release installers or plugin packages | `patterns/release-build.md` |
| Perform any recurring task | `patterns/INDEX.md` |

## Behavioural Contract

1. **CONTEXT** — Read the routed context and matching pattern; use current code/docs as truth if memory conflicts.
2. **BUILD** — Keep changes focused and preserve the non-negotiables in `AGENTS.md`.
3. **VERIFY** — Run every applicable item in `context/conventions.md`; plugin changes also use `scripts/check_plugin_host.ps1`.
4. **DEBUG** — Use the matching debug pattern, reproduce at the narrowest boundary, then rerun verification.
5. **GROW** — Update current state/context/patterns, bump `last_updated`, and record material decisions, risks, or todos with `mex log`.
