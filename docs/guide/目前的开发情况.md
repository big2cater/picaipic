# PicAiPic 目前的开发情况

> 2026-07-29 性能更新：CPU batch embedding 与批量事务已完成；11 万图库启动不再默认构建 ANN，精确矩阵搜索已实机验证；扫描内 folder-id cache 已实现，等待同样热重扫实测。

> 2026-07-27 更新（S1/S6 SQLite 加固 · EXIF/RAW fixture · 完整 Rust 验证；保留 7 月 26 日主题/扫描状态）：
>
> **分支：** `feat/black-hole-idle-theme`（PR：https://github.com/big2cater/picaipic/pull/3）
>
> **主题菜单（产品现状）**
> - **默认 / 复古 / CMYK / 黑洞 / 赛博朋克**（`THEME_ID` 0–4）。
> - 黑洞与赛博朋克：双钉 light/dark、`data-theme=dark`、appearance 锁定；共享 `dynamicThemeIntensity`（0/0.5/1/1.5）。
> - GridView **按主题挂载** WebGL 层（避免双 context 常驻）。
>
> **黑洞主题**
> - 宇宙层：`BlackHoleBackground` WebGL + Canvas2D 降级；Home 专用；TitleBar `z-50`；侧栏玻璃透出星空。
> - 触发：黑洞主题 + **主窗最大化** + **空闲 6s** + 无 reduced-motion / 空 inputStack / 非库切换。
> - 照片特效：`PhotoVortexLayer`（FragCoord UV 透镜；ResizeObserver 尺寸缓存；lazy GL）。
> - CSS `useGravityWarp` **保留未驱动**。
> - Runbook：`.mex/patterns/change-black-hole-theme.md`。
>
> **赛博朋克主题**
> - 日常：`CyberpunkBackground` 夜之城（网格/天际线/霓虹 + canvas **雨丝/粒子/假字**，**预烘焙 sprite**，无每帧 shadowBlur）。
> - 触发：与黑洞同七项 gate；`PhotoGlitchLayer` WebGL1 持续故障（**mediump 安全哈希** + `mod(time)`；强度 &gt;0；0 图不藏网格）。
> - Spec / plan：`docs/superpowers/specs/2026-07-26-cyberpunk-idle-glitch-design.md`、`docs/superpowers/plans/2026-07-26-cyberpunk-idle-glitch-impl.md`。
> - Runbook：`.mex/patterns/change-cyberpunk-theme.md`。
>
> **FX 正确性/性能（同日 follow-up）**
> - mediump 下 `sin*43758` 哈希会溢出 → 已换 fract 哈希；时间取模防长时间 idle 冻结。
> - 背景 sprite 化；vortex/glitch paint 不做每帧 `getBoundingClientRect`。
> - 缩放：`seedField(false)` 避免 RO 每帧重开雨场；glitch 捕获 rAF 可取消（`1aa0a59`）。
>
> **审查加固（2026-07-26）**
> - URL 导入超时 + 体积上限；复制入库失败清孤儿；恢复库原子写；插件 `is_path_inside`；embed warm；锁中毒容忍；扫描入口去重等。
> - 报告：`docs/review/code-review-2026-07-26.md`。
>
> **S1 / S6 SQLite 加固（2026-07-27）**
> - `AFile::new` 保留编排职责；已抽 header/EXIF/orientation/identity/descriptions/capture 与 RAW metadata merge policy。
> - 临时 SQLite fixture 覆盖 `insert/update/delete` 与 thumbnail 清理；最小 TIFF bytes 覆盖 IFD0/ExifIFD/UserComment/Rational/SRational/ISO；构造 `RawMeta` 锁定 EXIF 优先规则。
> - 这是结构和回归测试加固，不是新的性能优化；当前停止继续机械拆分 Live/Motion/geocode 分支。
> - 补充审查：`docs/review/code-review-2026-07-26-supplement.md`。
>
> **扫描 / RAW**
> - 缩略图成败均推进 `processed`；RAW **优先嵌入 JPEG**。Runbook：`fix-library-scan-selection.md`。
>
> **AI 扫描性能（2026-07-28）**
> - 提示词导入：JPEG 头部已完整扫描到 SOS/EOI 时不再重新打开文件扫描 COM；`AFile::new` 已解析的 EXIF `UserComment` 直接复用。截断头部仍走回退路径，原图和空评论优先级不变。
> - Embedding：vision ONNX 支持动态 batch，扫描 worker 默认最多聚合 8 张图后一次推理，并在一个 SQLite transaction 中批量写回 512 维向量、只失效一次搜索矩阵；准备、推理或事务失败仍会回退单文件。输入最长边保持 1024px，预处理直接写连续 NCHW，不再克隆整张解码图。
> - 实测 `D:\MultiModalKaggleDataset`：10,343 个文件（10,340 JPEG、3 PNG），缩略图和 embedding 均为 10,150 成功；当前完整分段基线为 **687.636 秒**。其中源图准备 239.678 秒、NCHW 预处理 130.617 秒、ONNX 推理 299.305 秒、SQLite 写回 1.937 秒、fallback 0。ONNX 是最大单项，但准备+预处理合计 370.295 秒，具备与推理重叠的空间。
> - Embedding worker 现直接携带扫描任务已有的路径/类型/方向，不再逐图完整回查 `AFile`；最多额外保留一批，在当前 batch 执行 ONNX 时为下一批解码并构建 NCHW。模型、batch 8、单 ONNX session、事务写回和单文件 fallback 均不变。`embedding_prefetched_batches` 用于确认实际重叠批数，真实总耗时等待下一次自然导入验证，不要求现在再次删相册。
> - 真实异构目录 `D:\Desktop` 验证：1,293 个文件完成扫描，1,255/1,255 次 embedding 成功，fallback 0，`embedding_prefetched_batches=36`，说明 bounded prefetch 正常且未丢任务。该目录不是同质 embedding A/B：915 批平均仅 1.37 张，`index_seconds=668.406` 占总计 672.863 秒的 99.3%。下一性能诊断转向 `index_single_file` 的 DB/metadata/media fallback 分段。
> - 同质一次性样本 `D:\PicAiPicPrefetchTest-20260728-235617`：1,000 个文件总计 **47.788 秒**，970/970 次 embedding 成功、fallback 0，123 批中 80 批发生预取，平均每批 7.89 张。prepare + engine + write 累计工作 65.071 秒并超过墙钟时间，证明流水线确有重叠；由于没有同一样本关闭预取的受控 A/B，不宣称精确加速百分比。
> - `index_single_file` opt-in 分段已实现：最终 `[scan-profile]` 现在输出 folder/fetch/stat/metadata/refresh/write/refetch/assemble/other；设置 `PICAIPIC_SCAN_SLOW_FILE_MS=250` 后，仅对超过阈值的文件输出 `[scan-slow-index]`。未开启 phase profile 时不执行这些分段时钟。
> - 新建复制样本 `D:\PicAiPicColdImportTest-20260729-115336` 的首次导入为 **44.662 秒**：`index_seconds=39.991`，其中 `AFile::new` 的 `index_metadata_seconds=37.379`；embedding engine 虽累计 40.972 秒但与遍历重叠，并非当前墙钟首要目标。现在仅在 `PICAIPIC_SCAN_PHASE_PROFILE=1` 下继续输出 metadata 的 file-info/inode、header、尺寸、EXIF、兼容回退、Motion XMP、HEIC、地理编码、prompt、组装子阶段。下一次请导入新的复制目录，不删除或重建任何已有相册；依据子阶段结果再改逻辑。
> - 对应的子阶段冷导入 `D:\PicAiPicColdImportTest-20260729-121925` 为 **45.172 秒**，最大项已定位为 binary EXIF fallback **17.172 秒**，其次为 EXIF parse **7.998 秒**、Motion XMP **4.731 秒**。binary fallback 原先对同一 128 KiB header 按多个 tag 重复全量扫描；现改为单次收集全部回退字段，完整 EXIF 优先级、Sony 方向和 Apple Live Photo `ContentIdentifier` 均保留。下一次只导入新的复制目录验收 binary 时间与成功数，再决定是否优化 Motion XMP。
> - binary 验收样本 `D:\PicAiPicColdImportTest-20260729-124718`：`index_seconds` **40.338 -> 24.591 秒**，binary fallback **17.172 -> 2.372 秒**（-86.2%），970/970 缩略图与 embedding 成功不变。总墙钟只到 **43.387 秒**，原因是更快的 producer 暴露了 `drain_seconds=16.693` 的后段等待。下一项 Motion XMP 现复用 128 KiB JPEG header；仅当 JPEG marker 已完整到达 SOS/EOI 才跳过旧的 512 KiB reopen，截断 header 与 HEIC 保留原读文件回退。
> - Motion 验证样本 `D:\PicAiPicColdImportTest-20260729-130119`：`index_seconds=22.618`、Motion XMP **3.067 秒**，但 `drain_seconds=18.967`，总墙钟仍为 **43.739 秒**。随后“推理期间持续凑满下一批”的实验虽达到 121/122 批预取，却严重回归：新复制样本总计 **100.724 秒**、`drain_seconds=73.209`，任务回复仅 **962/970** 成功。根因是通道仍开放时尾批等待凑足 8 张，延迟回复触发 60 秒 timeout；该实现已回退。后续 `0.758 秒` 的暖重扫无任何缩略图/embedding 任务，证实向量最终都已写入，未丢数据。当前保留安全的机会式满批预取与 3ms 尾批聚合，不能以预取批数单独判断性能。
> - 下一轮不再盲调并发：仅在 `PICAIPIC_SCAN_PHASE_PROFILE=1` 时新增 embedding 许可等待、通道发送等待、worker 回复等待，以及两类 timeout 计数；默认扫描零额外计时。用一个新的复制目录跑冷导入，再根据最大等待项决定下一项优化。
> - EXIF 拆分验收样本 `D:\PicAiPicColdImportTest-20260729-142730`：上轮 1,000 张样本的 EXIF 为 header parse **4.595 秒**、完整 JPEG 回退 **5.012 秒 / 785 次**；marker walk 证实 995 个 JPEG 头均已到 SOS/EOI，其中 780 个没有 EXIF APP1。现在只对“头完整且无 EXIF APP1”跳过不可能成功的重开扫描，不完整/非 JPEG/已有 EXIF 解析失败仍保留回退。验收回退降至 **5 次 / 0.044 秒**，EXIF **9.611 -> 4.563 秒**，index **25.881 -> 19.694 秒**，缩略图与 embedding **970/970** 成功；总时长 **42.949 -> 40.371 秒**，drain 增至 18.234 秒，下一项应在 header parse/字段提取中取证而非继续调回退 I/O。
> - EXIF header 内部拆分与 marker 快路径已验收：新样本 `D:\PicAiPicColdImportTest-20260729-154548` 显示 container reader 仅 **0.110 秒**、raw `to_vec`/解析为 **0**、`Exif\0\0`/TIFF 线性签名扫描为 **4.420 秒**。因此完整 JPEG marker 已到 SOS/EOI 且无 EXIF APP1 时，现直接跳过 permissive reader；截断、非 JPEG、已有 EXIF 仍保留旧回退。对应新样本 `D:\PicAiPicColdImportTest-20260729-160031` 将 EXIF header **4.532 -> 0.048 秒**、metadata **16.753 -> 12.979 秒**、index **19.841 -> 16.170 秒**，缩略图/embedding 仍为 **970/970**。总时长 **38.064 -> 39.341 秒**只因 drain **15.953 -> 20.921 秒**波动，不能归因给 EXIF。
> - 后续冷 metadata marker 路径均已验收：完整无 EXIF JPEG 不再重复扫描方向，orientation **4.086 -> 0.842 秒**；Motion XMP 内部拆分证明通用 header XMP 扫描占 **3.407/3.502 秒**，完整 JPEG 的 APP1 无 XMP namespace/`<x:xmpmeta>` 时现跳过该扫描，Motion **3.502 -> 0.149 秒**；binary 兜底拆分又证明 TIFF 签名扫描 **1.965 秒**，且完整无 EXIF JPEG 组没有 TIFF base，现跳过该组，binary **2.931 -> 1.004 秒**。最终新复制样本 `D:\PicAiPicColdImportTest-20260729-175121` 的 metadata **4.347 秒**、index **7.197 秒**，缩略图与 embedding 仍 **970/970**。总墙钟 **37.443 秒**受 `drain_seconds=28.296` 独立波动影响，不归因给 metadata；下一个目标先对 capture fallback、TIFF entry scan 或剩余方向 fallback 做内部取证。
> - 新冷导入已定位最大等待：embedding permit wait 累计 **10088.257 秒**，而 send 仅 **0.002 秒**、reply **720.324 秒**、两类 timeout 均为 0，`drain_seconds=19.902`。三批在途 probe 保持 **970/970**、timeout 仍为 0，permit wait 降至 **5816.884 秒**，`drain_seconds=13.601`（-31.7%），总时长 **46.360 -> 41.239 秒**（-11.0%）；已接受为默认值。`PICAIPIC_EMBED_INFLIGHT_BATCHES` 仍允许 2-4 供诊断，日常无需设置；不增加 ONNX session，也不等待尾批凑满。
> - 同一 1,000 张相册的热重扫耗时 **2.022 秒**：1,000 行全部命中已有索引，没有缩略图或 embedding 任务；folder 0.380 秒、fetch 0.461 秒、stat 0.115 秒、write 0.455 秒，metadata/refresh/refetch/assemble 均为 0。说明已有缩略图和向量会复用，剩余成本是目录/SQLite bookkeeping（含 mark-and-sweep 所需的扫描时间写回）。下一步测现有 `D:\Desktop` 相册热重扫，不删除相册。
> - `D:\Desktop` 热重扫同样只需 **2.643 秒**：folder 0.505 秒、fetch 0.610 秒、stat 0.145 秒、write 0.545 秒，无慢文件、无缩略图/embedding 任务。首次导入的 672 秒不是每次重扫成本。
> - 根据两份热重扫，worker 现仅在一次扫描内缓存 `folder path -> folder id`，同级文件不再重复 `AFolder::add_to_db`；扫描结束即释放，不改 SQLite schema 或目录语义。`[scan-profile]` 新增 `index_folder_cache_hits/misses`，下一次同样热重扫先验收命中数和实际总耗时，再决定是否优化 SQLite fetch/write。
> - folder-id cache 的同样 1,000 张热重扫已验收：总计 **2.022 -> 1.677 秒**（-17.1%），`index_seconds` **1.417 -> 1.059 秒**（-25.3%）；15 miss、985 hit，folder 时间 **0.380 -> 0.008 秒**。下一热路径是 SQLite fetch + mark-and-sweep timestamp write（合计 0.926 秒），但先用 `D:\Desktop` 热重扫验证缓存，再改写入语义。
> - `D:\Desktop` 已验证 folder-id cache：总计 **2.643 -> 2.198 秒**，`index_seconds` **1.813 -> 1.355 秒**；44 miss、1249 hit，folder 时间 **0.505 -> 0.018 秒**。两种目录结构均成立后，unchanged 文件的 `last_scan_time` 已改为最多 50 条一个 SQLite transaction，并在原有 progress checkpoint 之前提交；失败会阻止 stale cleanup，保证恢复与 mark-and-sweep 安全。新字段为 `index_seen_batch_items/batches/seconds`，等待同样热重扫实测。
> - seen timestamp 批量写入的 1,000 张实测：1,000 条分 20 个事务，`index_write_seconds` **0.454 -> 0.029 秒**（-93.6%）；`index_seconds` **1.059 -> 0.659 秒**（-37.8%），总计 **1.677 -> 1.303 秒**（-22.3%）。当前热重扫最大项是 SQLite fetch 0.510 秒，先验证 `D:\Desktop` 后再优化 lookup 路径。
> - SQLite fetch 后续已完成并在 `D:\Desktop` 验收：扫描开始时按相册一次性载入轻量 file state（id、目录/文件名 key、修改时间、thumb/embed 标志、方向、尺寸、duration），unchanged 文件不再逐个构造完整 `AFile` 查询；仍逐个 `stat`，仍走原有 50 条 seen 批量写回。1,293 张的 preload 为 **0.014 秒**、1293 hit / 0 miss、`index_fetch_seconds=0.000`；总计 **2.016 -> 1.068 秒**（-47.0%），`index_seconds` **0.954 -> 0.182 秒**（-80.9%）。新文件、已修改、缺缩略图或 preload 失败均回退原完整 lookup/update 路径；cache 只活在本次扫描，结束即释放、不落盘。热重扫剩余最大项是 `stat=0.153 秒`，先保留现状，除非后续代表性 profile 显示值得改。
> - 10k 验证也通过：`D:\MultiModalKaggleDataset` 的 10,343 张为 preload **0.111 秒**、10343 hit / 0 miss、`index_fetch_seconds=0.000`。新的主要耗时是 `stat=1.536 秒` 和 207 次 seen 写事务的 **1.130 秒**。因此 unchanged seen 写入与恢复 checkpoint 已从 50 提升到 **500** 项一批，顺序仍为“seen 事务成功后再保存 checkpoint”；异常时最多多重扫 499 个文件，不会错误清理旧记录。下一步只需重扫现有 10k 相册验收事务次数和耗时，**不要删除或重新添加相册**。
> - 500 项窗口的同相册验收完成：seen 事务 **207 -> 21**，写入 **1.130 -> 0.605 秒**（-46.5%），总计 **10.164 -> 8.786 秒**（-13.6%）；10,343 张仍为全量 cache hit、无 thumbnail/embedding 任务。当前热重扫最大项为 `stat=1.311 秒`，为确保本地盘、移动盘和变更文件均能正确识别，保留逐文件 stat，不再用高复杂度或弱一致性的方案继续挤压这部分时间。
> - `PICAIPIC_EMBED_BATCH_SIZE` 仅用于 1–32 的受控诊断，产品默认保持 8。首个 batch 16 结果为 2,173.659 秒且受到旧版 100k ANN 构建干扰，仍明显不够好，不提升默认值。
> - 解码健壮性：扩展名为 `.jpg` 但内容是 RIFF/WebP 时按内容识别；CMYK/YCCK JPEG 的 libjpeg-turbo 失败会回退通用 decoder，最终以成功数和总耗时判断。
> - 11 万图库验收：启动日志为 `embed_matrix warm ready ... n=110343 dim=512 ann=disabled`；重复搜索 `bird` 均走 `matrix=1`，扫描 110,343 个候选并稳定返回 30 条。默认不会调度 HNSW，也无需等待 `embed_ann ready`；`PICAIPIC_EMBED_ANN=1` 只保留给显式诊断。
> - Matrix warm 只是启动时从 SQLite 已有向量重建进程内精确搜索矩阵，不会重新解码或推理整库。搜索瞬间可能提高 CPU，但默认路径不会留下 ANN 后台构建。
>
> **当前验证步骤 / 下一步**
> 1. Matrix warm 可观测性已实现。默认 `embed_matrix warm ready` 增加 `elapsed_seconds` 与矩阵实际分配量 `matrix_mib`，不启用逐行计时。
> 2. 设置 `PICAIPIC_EMBED_WARM_PROFILE=1` 后重启现有 110,343 向量图库，会额外输出 `warm_profile`：SQLite step+BLOB 复制、向量解析/范数构建、总耗时、行数、矩阵分配量和进程工作集。warm 后先等待 5 秒，再用 5 秒窗口输出 `idle_profile` 的任务管理器口径 CPU 与工作集；这 10 秒内不要搜索或扫描。
> 3. 首轮 110,343 向量实测：总计 3.150 秒，其中 SQLite 1.217 秒、向量/范数构建 1.912 秒；warm 后五秒窗口进程 CPU 仅 0.06%。长期 90% CPU 问题已关闭，当前不做磁盘 cache。
> 4. 最终复测：单次 SQLite 扫描、直接借用 BLOB 切片解析（避免累计约 215.5 MiB 临时复制）并在构建结束后一次 `shrink_to_fit`，warm 为 **2.876 秒**，比 3.150 秒基线快 8.7%；矩阵为 **216.8 MiB**，比 257.5 MiB 少 40.7 MiB。SQLite 1.062 秒、build 1.804 秒，空闲 CPU 0.00%；启动链路优化结束。
> 5. 本轮只需重启，不删除相册、不重新导入。ANN 仍不恢复默认；之后转向 prepare/ONNX engine 与解码复用。DirectML 仍只允许独立 runtime/subprocess 探针。
> 6. Engine 分段已完成并据此实现单批 prefetch；`D:\Desktop` 已验证成功数、fallback 和实际 prefetch 批数。prefetch 启用后 prepare/preprocess/inference 是可重叠的累计工作量，不能相加当墙钟时间。
> 7. `index_single_file` 分段、慢文件日志、扫描内 folder-id cache 与有界 seen timestamp 批量写入已完成，1,000 张样本均已验证。下一步热重扫 `D:\Desktop`，设置 `$env:PICAIPIC_SCAN_PHASE_PROFILE="1"` 和 `$env:PICAIPIC_SCAN_SLOW_FILE_MS="250"`，确认 cache/seen batch 字段、总耗时与成功数；不需要删除相册重导。
>
> **验证**
> - Rust：`cargo check` 通过；SQLite/search suite **37 passed / 2 ignored**；slow-file 配置、prefetch channel、batch transaction、NCHW layout、ANN opt-in 配置定向测试通过。
> - 主题历史验证：`node scripts/check_theme_ids.mjs`；`mex check`（0 errors）。
> - 手测：两主题 + 最大化 + 6s idle；长 idle glitch 仍动；强度 0 不冻结；切主题卸载 GL。
>
> 会话路由：`.mex/ROUTER.md`。
>
> ---
>
> 2026-07-24 及更早（智能相册 · 智能标签 · CLIP/搜索矩阵 · 内置工具 · 沙箱等）见下文历史段落与 `docs/guide/picaipic-progress.md`。

---

## 历史摘要（2026-07-24 及以前）

### 智能相册 / 智能标签 / 搜索
- 智能相册 size 算子、选择器、本地日历日、空态引导 — `change-smart-albums.md`
- 智能标签 6 类 + 默认 High — `change-smart-tags.md`
- Track C 中英文本塔默认；找相似图图-图分路；精确 embed 矩阵默认、HNSW 仅 opt-in — `change-ai-search-filters.md` / `change-library-perf.md` / `change-image-search-model.md`

### 内置工具 / 安全 / 发布
- 裁剪 / 拼图 / 批处理 / 冲印 / 追色 / 照片格调 / 相框 — `docs/guide/builtin-tools-roadmap.md`
- 插件签名与沙箱 Phase 0–2 默认；3–5 可选 — `docs/ai-plugin-sandbox-roadmap.md`
- v1.1.0 私有 draft 多架构包；发布策略见 `patterns/release-build.md`

### 明确不做（业主 2026-07-20）
- G1 拼图进批处理、G7 SA-LUT export、G8 人脸 GPU EP、G9 全库空评论回填

更细英文看板：`docs/guide/picaipic-progress.md`。
